---
layout: ../../../layouts/MdxLayout.astro
title: 线程组
parent:
  name: 返回上级
  path: /cheatsheet/java/concurrency
---

线程组（_thread group_）是一个线程的集合。一个线程组可以包含其他线程组，所以线程组是一个树形结构。

线程组 API 在 Java 早期的时候，提供了线程分组和任务控制功能。线程组支持隔离 applet 应用程序，定义了用于诊断目的的 API。新应用应该避免使用线程组 API。

真好，又不用学了。