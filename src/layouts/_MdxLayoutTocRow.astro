---
type Props = { text: string, slug: string };

const { text, slug } = Astro.props;
---
<a class="cursor-pointer text-indigo-700 hover:text-indigo-900 hover:bg-indigo-100" href={"#" + slug}>{text}</a>